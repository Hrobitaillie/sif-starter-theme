/**
 * Bloc CTA Triple
 */

class CtaTriple extends SifBlock {
  do({ block, index, uuid }) {
    // Code à exécuter pour chaque instance du bloc
  }
}

new CtaTriple({
  selector: ".wp-block-sif-cta-triple",
});
